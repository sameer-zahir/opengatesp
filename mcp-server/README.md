# OpenGateSP MCP server (roadmap)

Placeholder — not built yet.

The planned MCP (Model Context Protocol) server will expose each OpenGateSP engine function
as a tool, so Claude / Codex / Gemini can run migrations and reports conversationally. It will
call the PowerShell engine and consume each function's `-AsJson` output, so little new logic is
needed. See [../docs/roadmap.md](../docs/roadmap.md).
